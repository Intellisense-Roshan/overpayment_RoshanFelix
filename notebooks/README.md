# Notebooks

Use this folder for exploratory data analysis (EDA), pattern discovery, and prototype visual diagnostics.

## Conventions
- Keep notebooks reproducible from top to bottom.
- Import core reusable logic from `src.` rather than duplicating helper code.
- Do not modify raw CSV files in `data/`.
